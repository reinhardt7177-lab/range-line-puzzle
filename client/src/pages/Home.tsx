import { useEffect, useMemo, useState } from "react";
import { ArrowLeft, ArrowRight, BookOpen, Check, ChevronRight, Clock3, Crown, Flag, HelpCircle, Lightbulb, Lock, Map, RotateCcw, ShieldCheck, Sparkles, Target, Trophy, X } from "lucide-react";

// Design reminder: quest map for a serious study app. Story frames the work;
// one question at a time, clear theory, calm record keeping, no arcade pressure.

type QuestQuestion = { prompt: string; options: string[]; answers: string[]; multiple?: boolean; explanation: string; hint: string };
type Quest = { id: number; title: string; place: string; concept: string; summary: string; gate: string; tone: "blue" | "mint" | "orange" | "purple"; theory: string; questions: QuestQuestion[] };
type RecordValue = { best?: number; last?: number; accuracy: number; attempts: number; hints: number; completed: boolean };
type Records = Record<number, RecordValue>;

const quests: Quest[] = [
  { id: 1, title: "닫힌 문 앞에서", place: "수의 문", concept: "범위가 필요한 상황", summary: "정확한 수, 범위, 어림 중 알맞은 표현을 찾아요.", gate: "범위의 눈", tone: "blue", theory: "수의 범위는 어떤 수가 기준값보다 큰지, 작은지, 또는 기준값을 포함하는지 나타내요.", questions: [{ prompt: "다음 중 ‘수의 범위’로 나타내는 것이 가장 알맞은 상황은 무엇일까요?", options: ["오늘의 정확한 시각", "키가 140cm 이상인 학생", "연필의 정확한 길이", "계산기의 정확한 답"], answers: ["키가 140cm 이상인 학생"], explanation: "‘키가 140cm 이상’처럼 기준에 따라 여러 수를 묶어 나타낼 때 수의 범위를 사용해요.", hint: "기준값을 중심으로 여러 수를 묶는 상황을 찾아보세요." }] },
  { id: 2, title: "채워진 점의 문", place: "이상·이하", concept: "기준값 포함", summary: "이상과 이하에서 기준값도 함께 생각해요.", gate: "채워진 점", tone: "mint", theory: "이상은 기준값과 같거나 큰 수, 이하는 기준값과 같거나 작은 수예요. 기준값을 포함해요.", questions: [{ prompt: "‘120 이상’인 수를 모두 고르세요.", options: ["112", "120", "127", "98"], answers: ["120", "127"], multiple: true, explanation: "이상은 기준값을 포함하고 그보다 큰 수예요. 120과 127이 해당해요.", hint: "‘이상’은 기준값도 포함해요." }] },
  { id: 3, title: "빈 점의 숲", place: "초과·미만", concept: "기준값 제외", summary: "초과와 미만은 기준값을 포함하지 않아요.", gate: "빈 점", tone: "purple", theory: "초과는 기준값보다 큰 수, 미만은 기준값보다 작은 수예요. 기준값은 포함하지 않아요.", questions: [{ prompt: "‘85 미만’인 수를 고르세요.", options: ["85", "84", "90", "76"], answers: ["84", "76"], multiple: true, explanation: "미만은 기준값을 포함하지 않고 그보다 작은 수예요. 84와 76이 해당해요.", hint: "85와 같은 수는 미만에 들어갈 수 없어요." }] },
  { id: 4, title: "경계의 다리", place: "수직선", concept: "범위 나타내기", summary: "채워진 점·빈 점과 화살표 방향을 연결해요.", gate: "수직선 관문", tone: "blue", theory: "기준값을 포함하면 채워진 점(●), 포함하지 않으면 빈 점(○)으로 나타내요.", questions: [{ prompt: "‘40 초과’를 수직선으로 나타낼 때 알맞은 설명은 무엇일까요?", options: ["40에서 채워진 점, 왼쪽 방향", "40에서 빈 점, 왼쪽 방향", "40에서 채워진 점, 오른쪽 방향", "40에서 빈 점, 오른쪽 방향"], answers: ["40에서 빈 점, 오른쪽 방향"], explanation: "초과는 기준값을 포함하지 않으므로 빈 점, 기준값보다 큰 쪽이므로 오른쪽 방향이에요.", hint: "초과는 빈 점이고, 큰 수가 있는 쪽으로 가요." }] },
  { id: 5, title: "부족하지 않은 창고", place: "올림", concept: "부족하지 않게", summary: "모두 담거나 부족하지 않게 준비할 때 올림해요.", gate: "창고 관문", tone: "orange", theory: "올림은 구하려는 자리 아래 수를 올려서 나타내요. 필요한 양보다 부족하지 않아야 할 때 사용해요.", questions: [{ prompt: "한 상자에 사과 10개씩 담습니다. 사과 83개를 모두 담으려면 상자는 몇 개 필요할까요?", options: ["8개", "9개", "10개", "80개"], answers: ["9개"], explanation: "83개를 모두 담아야 하므로 부족하지 않게 올림해요. 10개 상자 9개가 필요해요.", hint: "8상자는 80개만 담을 수 있어요. 모두 담으려면 하나가 더 필요해요." }] },
  { id: 6, title: "남김없는 작업장", place: "버림", concept: "완전한 묶음", summary: "완전하게 만들 수 있는 묶음만 생각할 때 버림해요.", gate: "작업장 관문", tone: "orange", theory: "버림은 구하려는 자리 아래 수를 버려서 나타내요. 완전한 묶음의 수만 필요할 때 사용해요.", questions: [{ prompt: "764cm의 리본으로 10cm 묶음을 최대 몇 개 만들 수 있을까요?", options: ["7개", "76개", "77개", "760개"], answers: ["76개"], explanation: "10cm 묶음은 76개를 완전히 만들 수 있어요. 남은 4cm는 완전한 묶음이 아니므로 버려요.", hint: "10cm씩 묶을 수 있는 완전한 묶음만 세어 보세요." }] },
  { id: 7, title: "가장 가까운 광장", place: "반올림", concept: "가장 가까운 값", summary: "가장 가까운 값으로 나타낼 때 반올림해요.", gate: "광장 관문", tone: "mint", theory: "반올림은 바로 아래 자리 숫자가 5 이상이면 올리고, 5보다 작으면 버려요.", questions: [{ prompt: "3,650을 천의 자리까지 반올림한 수를 고르세요.", options: ["3,000", "3,600", "4,000", "3,700"], answers: ["4,000"], explanation: "천의 자리 바로 아래인 백의 자리 숫자가 6이에요. 5 이상이므로 올려서 4,000이 돼요.", hint: "천의 자리 바로 아래인 백의 자리 6을 보세요." }] },
  { id: 8, title: "시간의 지도", place: "종합 관문", concept: "방법 선택", summary: "상황의 목적에 따라 올림·버림·반올림을 선택해요.", gate: "시간의 지도", tone: "purple", theory: "부족하면 안 되면 올림, 완전한 묶음이면 버림, 가장 가까운 값이면 반올림이에요.", questions: [{ prompt: "학급 학생 287명의 대략적인 인원을 백의 자리까지 나타내려고 합니다. 어떤 방법이 알맞을까요?", options: ["올림", "버림", "반올림", "이상"], answers: ["반올림"], explanation: "대략적인 인원을 가장 가까운 값으로 나타내므로 반올림이 알맞아요. 287명은 약 300명이에요.", hint: "가장 가까운 값을 구할 때 사용하는 방법을 생각해 보세요." }] },
];

const emptyRecords = (): Records => Object.fromEntries(quests.map((quest) => [quest.id, { accuracy: 0, attempts: 0, hints: 0, completed: false }])) as Records;
const formatTime = (seconds: number) => `${String(Math.floor(seconds / 60)).padStart(2, "0")}:${String(seconds % 60).padStart(2, "0")}`;

function QuestEmblem({ tone, done = false, locked = false }: { tone: Quest["tone"]; done?: boolean; locked?: boolean }) {
  return <div className={`quest-emblem quest-emblem--${tone} ${done ? "quest-emblem--done" : ""} ${locked ? "quest-emblem--locked" : ""}`}>{locked ? <Lock size={20} /> : done ? <Check size={22} /> : <Target size={21} />}</div>;
}

export default function Home() {
  const [records, setRecords] = useState<Records>(() => emptyRecords());
  const [loaded, setLoaded] = useState(false);
  const [screen, setScreen] = useState<"map" | "brief" | "play" | "result">("map");
  const [activeQuestId, setActiveQuestId] = useState(1);
  const [questionIndex, setQuestionIndex] = useState(0);
  const [selected, setSelected] = useState<string[]>([]);
  const [submitted, setSubmitted] = useState(false);
  const [correct, setCorrect] = useState(false);
  const [attempts, setAttempts] = useState(0);
  const [hints, setHints] = useState(0);
  const [startedAt, setStartedAt] = useState<number | null>(null);
  const [elapsed, setElapsed] = useState(0);
  const [lastResult, setLastResult] = useState<RecordValue | null>(null);
  const [showHint, setShowHint] = useState(false);

  useEffect(() => { try { const saved = window.localStorage.getItem("boundary-quest-records"); if (saved) setRecords({ ...emptyRecords(), ...JSON.parse(saved) }); } finally { setLoaded(true); } }, []);
  useEffect(() => { if (loaded) window.localStorage.setItem("boundary-quest-records", JSON.stringify(records)); }, [records, loaded]);
  useEffect(() => { if (screen !== "play" || !startedAt) return; const interval = window.setInterval(() => setElapsed(Math.floor((Date.now() - startedAt) / 1000)), 1000); return () => window.clearInterval(interval); }, [screen, startedAt]);

  const quest = quests[activeQuestId - 1];
  const question = quest.questions[questionIndex];
  const completedCount = quests.filter((item) => records[item.id]?.completed).length;
  const unlocked = (id: number) => id === 1 || records[id - 1]?.completed;
  const activeRecord = records[activeQuestId] ?? emptyRecords()[activeQuestId];
  const currentCorrect = selected.length > 0 && question.answers.length === selected.length && question.answers.every((answer) => selected.includes(answer));

  const openQuest = (id: number) => { if (!unlocked(id)) return; setActiveQuestId(id); setScreen("brief"); setQuestionIndex(0); setSelected([]); setSubmitted(false); setCorrect(false); setShowHint(false); };
  const startQuest = () => { const now = Date.now(); setStartedAt(now); setElapsed(0); setAttempts(0); setHints(0); setQuestionIndex(0); setSelected([]); setSubmitted(false); setCorrect(false); setShowHint(false); setScreen("play"); };
  const submit = () => { if (selected.length === 0) return; const value = currentCorrect; setAttempts((count) => count + 1); setCorrect(value); setSubmitted(true); };
  const retry = () => { setSelected([]); setSubmitted(false); setCorrect(false); setShowHint(false); };
  const nextQuestion = () => { if (questionIndex < quest.questions.length - 1) { setQuestionIndex((index) => index + 1); retry(); } else finishQuest(); };
  const finishQuest = () => { const finalSeconds = startedAt ? Math.floor((Date.now() - startedAt) / 1000) : elapsed; const accuracy = correct ? 100 : 0; const previous = records[activeQuestId] ?? emptyRecords()[activeQuestId]; const nextRecord: RecordValue = { best: previous.best ? Math.min(previous.best, finalSeconds) : finalSeconds, last: finalSeconds, accuracy, attempts: attempts + 1, hints, completed: true }; setRecords((current) => ({ ...current, [activeQuestId]: nextRecord })); setLastResult(nextRecord); setScreen("result"); };
  const toggleOption = (option: string) => { if (submitted) return; if (question.multiple) setSelected((current) => current.includes(option) ? current.filter((item) => item !== option) : [...current, option]); else setSelected([option]); };

  if (screen === "brief") return <BriefScreen quest={quest} record={activeRecord} onBack={() => setScreen("map")} onStart={startQuest} />;
  if (screen === "play") return <PlayScreen quest={quest} question={question} questionIndex={questionIndex} selected={selected} submitted={submitted} correct={correct} elapsed={elapsed} attempts={attempts} hints={hints} showHint={showHint} onBack={() => setScreen("map")} onToggle={toggleOption} onSubmit={submit} onRetry={retry} onNext={nextQuestion} onHint={() => { setShowHint((value) => !value); if (!showHint) setHints((value) => value + 1); }} />;
  if (screen === "result" && lastResult) return <ResultScreen quest={quest} result={lastResult} onMap={() => setScreen("map")} onNext={() => { const nextId = Math.min(8, activeQuestId + 1); setActiveQuestId(nextId); setScreen(unlocked(nextId) ? "brief" : "map"); }} />;
  return <MapScreen records={records} completedCount={completedCount} activeQuestId={activeQuestId} onOpen={openQuest} />;
}

function Header({ onMap, title = "경계의 성" }: { onMap?: () => void; title?: string }) {
  return <header className="quest-header"><button type="button" className="quest-brand" onClick={onMap}><span className="quest-brand__mark"><Map size={19} /></span><span><small>UNIT 01 · 1단원</small><strong>{title}</strong></span></button><div className="quest-header__center"><span>사라진 시간의 지도</span><ChevronRight size={14} /><b>8개 관문</b></div><div className="quest-header__profile"><span>나의 기록</span><strong>J</strong></div></header>;
}

function MapScreen({ records, completedCount, activeQuestId, onOpen }: { records: Records; completedCount: number; activeQuestId: number; onOpen: (id: number) => void }) {
  return <main className="quest-app"><Header /><section className="map-page"><div className="map-intro"><div><p className="quest-kicker">THE LOST TIME MAP</p><h1>경계의 성을<br /><em>되찾아 주세요.</em></h1><p>한 문제씩 관문을 통과하면<br />사라진 시간의 지도가 다시 이어져요.</p></div><div className="map-progress"><div className="map-progress__icon"><Crown size={23} /></div><div><span>QUEST PROGRESS</span><strong>{completedCount} <small>/ 8</small></strong><i><b style={{ width: `${(completedCount / 8) * 100}%` }} /></i></div></div></div><div className="quest-map"><div className="map-route" />{quests.map((item, index) => { const done = records[item.id]?.completed; const locked = !((item.id === 1) || records[item.id - 1]?.completed); const current = activeQuestId === item.id && !done; return <button type="button" key={item.id} className={`quest-node quest-node--${item.tone} ${done ? "quest-node--done" : ""} ${locked ? "quest-node--locked" : ""} ${current ? "quest-node--current" : ""}`} style={{ "--node-x": `${14 + (index % 4) * 24}%`, "--node-y": `${index % 2 === 0 ? 23 : 60}%` } as React.CSSProperties} onClick={() => onOpen(item.id)}><QuestEmblem tone={item.tone} done={done} locked={locked} /><span className="quest-node__text"><b>{String(item.id).padStart(2, "0")} · {item.place}</b><strong>{item.title}</strong><small>{done ? `최고 ${formatTime(records[item.id].best ?? 0)}` : locked ? "이전 관문을 먼저 통과하세요" : item.concept}</small></span>{current && <span className="current-pill">NEXT</span>}</button>; })}<div className="map-landmark map-landmark--start"><span>START</span><ShieldCheck size={18} /></div><div className="map-landmark map-landmark--finish"><Trophy size={24} /><span>시간의 지도</span></div></div><div className="map-footer"><span><Clock3 size={15} /> 기록은 나의 최고 기록으로 남아요.</span><span><Target size={15} /> 정확도 우선 · 속도는 나와 비교하기</span></div></section></main>;
}

function BriefScreen({ quest, record, onBack, onStart }: { quest: Quest; record: RecordValue; onBack: () => void; onStart: () => void }) {
  return <main className="quest-app"><Header onMap={onBack} /><section className="brief-page"><div className="brief-visual"><div className={`brief-orb brief-orb--${quest.tone}`}><QuestEmblem tone={quest.tone} /></div><div className="brief-path"><i /><i /><i /></div><div className="brief-landmark"><Flag size={23} /></div><span className="brief-label brief-label--a">{quest.place}</span><span className="brief-label brief-label--b">GATE {String(quest.id).padStart(2, "0")}</span></div><div className="brief-content"><button type="button" className="back-link" onClick={onBack}><ArrowLeft size={15} /> 퀘스트 맵으로</button><p className="quest-kicker">QUEST {String(quest.id).padStart(2, "0")} · {quest.gate}</p><h1>{quest.title}</h1><p className="brief-summary">{quest.summary}</p><div className="brief-theory"><BookOpen size={19} /><div><span>이번 관문에서 알아야 할 것</span><strong>{quest.concept}</strong><p>{quest.theory}</p></div></div><div className="brief-rule"><Lightbulb size={17} /><span>문제를 한 개씩 풀어 마지막 관문까지 도착하세요. 완료 시간과 정확도가 기록됩니다.</span></div><button type="button" className="start-quest" onClick={onStart}>퀘스트 시작하기 <ArrowRight size={18} /></button>{record.completed && <div className="previous-record"><span>지난 기록</span><b>{formatTime(record.last ?? 0)}</b><small>최고 {formatTime(record.best ?? 0)} · 정확도 {record.accuracy}%</small></div>}</div></section></main>;
}

function PlayScreen({ quest, question, questionIndex, selected, submitted, correct, elapsed, attempts, hints, showHint, onBack, onToggle, onSubmit, onRetry, onNext, onHint }: { quest: Quest; question: QuestQuestion; questionIndex: number; selected: string[]; submitted: boolean; correct: boolean; elapsed: number; attempts: number; hints: number; showHint: boolean; onBack: () => void; onToggle: (option: string) => void; onSubmit: () => void; onRetry: () => void; onNext: () => void; onHint: () => void }) {
  return <main className="quest-app"><Header onMap={onBack} title={quest.title} /><div className="play-layout"><aside className="play-rail"><div className={`rail-seal rail-seal--${quest.tone}`}><QuestEmblem tone={quest.tone} /></div><p className="quest-kicker">QUEST {String(quest.id).padStart(2, "0")}</p><h1>{quest.place}<br /><em>관문</em></h1><p>{quest.concept}</p><div className="rail-time"><Clock3 size={16} /><span>경과 시간</span><strong>{formatTime(elapsed)}</strong></div><div className="rail-steps">{quest.questions.map((_, index) => <div key={index} className={index < questionIndex ? "rail-step rail-step--done" : index === questionIndex ? "rail-step rail-step--current" : "rail-step"}><span>{index < questionIndex ? <Check size={12} /> : index + 1}</span><b>{index === questionIndex ? "현재 문제" : index < questionIndex ? "통과" : "대기"}</b></div>)}</div><div className="rail-record"><span>이번 시도</span><strong>{attempts}회</strong><small>힌트 {hints}회</small></div></aside><section className="play-main"><div className="play-top"><div><p className="quest-kicker">{quest.gate}</p><h2>관문을 하나씩 통과하세요.</h2></div><div className="play-counter"><span>{questionIndex + 1}</span><small>/ {quest.questions.length}</small></div></div><div className="question-sheet"><div className="sheet-header"><span className={`quest-tag quest-tag--${quest.tone}`}>{quest.concept}</span><span className="sheet-status">문제 {questionIndex + 1}</span></div><h3>{question.prompt}</h3>{question.multiple && <p className="multi-note">정답을 모두 선택하세요.</p>}<div className="quest-options">{question.options.map((option, index) => <button type="button" key={option} disabled={submitted} className={`quest-option ${selected.includes(option) ? "quest-option--selected" : ""} ${submitted && question.answers.includes(option) ? "quest-option--correct" : ""} ${submitted && selected.includes(option) && !question.answers.includes(option) ? "quest-option--wrong" : ""}`} onClick={() => onToggle(option)}><span>{String.fromCharCode(65 + index)}</span><strong>{option}</strong>{selected.includes(option) && !submitted && <Check size={16} />}{submitted && question.answers.includes(option) && <Check size={16} />}{submitted && selected.includes(option) && !question.answers.includes(option) && <X size={16} />}</button>)}</div><div className="sheet-actions"><button type="button" className="hint-action" onClick={onHint}><Lightbulb size={16} /> 힌트 {hints > 0 ? `${hints}회` : "보기"}</button>{!submitted ? <button type="button" className="answer-action" onClick={onSubmit} disabled={!selected.length}>정답 제출 <ArrowRight size={16} /></button> : correct ? <button type="button" className="answer-action answer-action--success" onClick={onNext}>{questionIndex === quest.questions.length - 1 ? "관문 완료" : "다음 문제"} <ArrowRight size={16} /></button> : <button type="button" className="retry-action" onClick={onRetry}><RotateCcw size={15} /> 다시 풀기</button>}</div>{showHint && <div className="quest-hint"><Lightbulb size={16} /><span><b>힌트</b>{question.hint}</span></div>}{submitted && <div className={correct ? "quest-feedback quest-feedback--success" : "quest-feedback quest-feedback--wrong"}><span>{correct ? <Check size={18} /> : <X size={18} />}</span><div><strong>{correct ? "관문에 빛이 켜졌어요." : "아직 이 문은 열리지 않아요."}</strong><p>{question.explanation}</p></div></div>}</div><div className="play-bottom"><span><ShieldCheck size={15} /> 정확하게 통과하는 것이 가장 중요해요.</span><span>개인 기록 저장 중</span></div></section></div></main>;
}

function ResultScreen({ quest, result, onMap, onNext }: { quest: Quest; result: RecordValue; onMap: () => void; onNext: () => void }) {
  return <main className="quest-app"><Header onMap={onMap} /><section className="result-page"><div className={`result-emblem result-emblem--${quest.tone}`}><Trophy size={34} /></div><p className="quest-kicker">QUEST CLEARED · {quest.gate}</p><h1>{quest.title}<br /><em>관문 통과!</em></h1><p className="result-copy">한 문제씩 정확하게 풀어서 {quest.place}을 통과했어요. 다음 지도 조각이 모습을 드러냈습니다.</p><div className="result-stats"><div><Clock3 size={19} /><span>이번 기록</span><strong>{formatTime(result.last ?? 0)}</strong></div><div><Target size={19} /><span>정확도</span><strong>{result.accuracy}%</strong></div><div><Sparkles size={19} /><span>최고 기록</span><strong>{formatTime(result.best ?? 0)}</strong></div></div><div className="result-note"><Crown size={17} /><span>{result.last === result.best ? "첫 기록이 곧 최고 기록이에요." : "나의 최고 기록과 비교해 보세요."}</span></div><div className="result-actions"><button type="button" className="map-button" onClick={onMap}>퀘스트 맵 보기</button>{quest.id < 8 && <button type="button" className="next-quest" onClick={onNext}>다음 관문으로 <ArrowRight size={17} /></button>}</div></section></main>;
}
